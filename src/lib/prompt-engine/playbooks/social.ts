import type { Playbook } from "./index.ts";

export const social: Playbook = {
  id: "social",
  title: "Social post / ad creative",
  guidance: `State the platform and ratio only when the user named them or the platform format is unambiguous. Design for a small screen: one strong focal point, high contrast, short copy, generous safe margins so nothing important touches the edges.
Quote exact copy and give it a hierarchy (hook, supporting line, call to action) and placement. Keep text brief; long paragraphs do not render well.
Describe the visual (photo, illustration, product, typographic) in one or two sentences with palette and mood. For carousels or a set of matching posts, name the shared elements (palette, type treatment, layout grid, logo position) that must be identical across every item, and what changes per item.
End with the text-control sentence: only the quoted copy may appear, spelled exactly; no extra hashtags, handles, or watermarks.`,
};
